extends base.pug

block content
    h4 changing title of video
    form(method="POST")
        input(name="title", placeholder="title", value=video.title, required)
        input(value="save", type="submit")
